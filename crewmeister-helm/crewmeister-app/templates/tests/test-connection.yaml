apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crewmeister-app.fullname" . }}-test-connection"
  labels:
    {{- include "crewmeister-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crewmeister-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
